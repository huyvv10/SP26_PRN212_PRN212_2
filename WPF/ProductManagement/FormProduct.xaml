<Window x:Name="frmFormProduct" x:Class="ProductManagement.FormProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProductManagement"
        mc:Ignorable="d"
        Title="Product Management" Height="480" Width="800" WindowStartupLocation="CenterScreen" Loaded="frmFormProduct_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

           <Border Grid.Column="0" Padding="15" 
                BorderBrush="Gray" BorderThickness="1" CornerRadius="5">

            <StackPanel>

                <TextBlock Text="PRODUCT INFORMATION"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 0 0 20"/>

                <Label Content="Product ID"/>
                <TextBox x:Name="txtPId"/>

                <Label Content="Product Name"/>
                <TextBox x:Name="txtPName"/>

                <Label Content="Unit"/>
                <TextBox x:Name="txtUnit"/>

                <Label Content="Quantity"/>
                <TextBox x:Name="txtQty"/>

                <Label Content="Price"/>
                <TextBox x:Name="txtPrice"/>

                <Label Content="Category"/>
                <ComboBox x:Name="cbCategory"
                          DisplayMemberPath="CatName"
                          SelectedValuePath="CatId"/>

                <StackPanel Orientation="Horizontal" Margin="0 20 0 0">
                    <Button x:Name="btnAdd" Content="_Add" Width="70" Margin="5" Click="btnAdd_Click"/>
                    <Button x:Name="btnUpdate" Content="_Update" Width="70" Margin="5"/>
                    <Button x:Name="btnDelete" Content="Delete" Width="70" Margin="5" Click="btnDelete_Click"/>
                    <Button x:Name="btnClose" Content="_Close" Width="70" Margin="5" Click="btnClose_Click"/>
                </StackPanel>

            </StackPanel>
        </Border>

         <Border Grid.Column="1" Margin="10 0 0 0"
                BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5,0,5">
                    <TextBox x:Name="txtSearch" Width="250" Margin="5,0,10,0"/>
                    <Button x:Name="btnSearch" Content="_Search" Width="150" Background="Yellow" Click="btnSearch_Click" />
                </StackPanel>
                <DataGrid x:Name="dgProducts"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single" Grid.Row="1" SelectionChanged="dgProducts_SelectionChanged">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding PId}" Width="1*"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding PName}" Width="3*"/>
                        <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="1*"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Qty}" Width="1*"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="1*"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Cat.CatName}" Width="2*"/>
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>
        </Border>

    </Grid>
 
    
</Window>
