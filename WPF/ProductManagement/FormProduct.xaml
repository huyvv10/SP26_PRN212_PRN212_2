<Window x:Name="frmFormProduct" x:Class="ProductManagement.FormProduct"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ProductManagement"
        mc:Ignorable="d"
        Title="Product Management" Height="480" Width="800" WindowStartupLocation="CenterScreen" Loaded="frmFormProduct_Loaded">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="350"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

           <Border Grid.Column="0" Padding="15" 
                BorderBrush="Gray" BorderThickness="1" CornerRadius="5">

            <StackPanel>

                <TextBlock Text="PRODUCT INFORMATION"
                           FontSize="18"
                           FontWeight="Bold"
                           Margin="0 0 0 20"/>

                <Label Content="Product ID"/>
                <TextBox x:Name="txtPId"/>

                <Label Content="Product Name"/>
                <TextBox x:Name="txtPName"/>

                <Label Content="Unit"/>
                <TextBox x:Name="txtUnit"/>

                <Label Content="Quantity"/>
                <TextBox x:Name="txtQty"/>

                <Label Content="Price"/>
                <TextBox x:Name="txtPrice"/>

                <Label Content="Category"/>
                <ComboBox x:Name="cbCategory"
                          DisplayMemberPath="CatName"
                          SelectedValuePath="CatId"/>

                <StackPanel Orientation="Horizontal" Margin="0 20 0 0">
                    <Button Content="Add" Width="70" Margin="5"/>
                    <Button Content="Update" Width="70" Margin="5"/>
                    <Button Content="Delete" Width="70" Margin="5"/>
                    <Button Content="Clear" Width="70" Margin="5"/>
                </StackPanel>

            </StackPanel>
        </Border>

         <Border Grid.Column="1" Margin="10 0 0 0"
                BorderBrush="Gray" BorderThickness="1" CornerRadius="5">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,5,0,5">
                    <TextBox x:Name="txtSearch" Width="250" Margin="5,0,10,0"/>
                    <Button x:Name="btnSearch" Content="_Search" Width="150" Background="Yellow" />
                </StackPanel>
                <DataGrid x:Name="dgProducts"
                      AutoGenerateColumns="False"
                      IsReadOnly="True"
                      SelectionMode="Single" Grid.Row="1">

                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding PId}" Width="80"/>
                        <DataGridTextColumn Header="Name" Binding="{Binding PName}" Width="150"/>
                        <DataGridTextColumn Header="Unit" Binding="{Binding Unit}" Width="80"/>
                        <DataGridTextColumn Header="Qty" Binding="{Binding Qty}" Width="80"/>
                        <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="100"/>
                        <DataGridTextColumn Header="Category" Binding="{Binding Cat.CatName}" Width="150"/>
                    </DataGrid.Columns>

                </DataGrid>
            </Grid>
        </Border>

    </Grid>
 
    
</Window>
